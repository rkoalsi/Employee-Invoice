import React from 'react';
import Login from '../auth/Login';

interface Props {}

function Home(props: Props) {
  const {} = props;

  return (
    <div>
      <Login />
    </div>
  );
}

export default Home;
